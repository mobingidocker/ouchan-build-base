Base image for `ouchan` CI builds.

Starting `prepare_v2.4`, and `build_v2.6` (and up), artifacts are now uploaded to [ghcr.io](https://github.com/mobingidocker/ouchan-build-base/pkgs/container/ouchan-build-base) instead of Docker Hub.
